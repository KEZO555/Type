<?xml version="1.0" encoding="utf-8"?>
<!-- IME metadata. Two B/W subtypes — English and Hebrew — so the system language switcher knows we
     handle both. In-keyboard a long-press on the space bar toggles them; the IME also reacts to an OS
     subtype switch. -->
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:supportsSwitchingToNextInputMethod="true">
    <subtype
        android:label="@string/subtype_en"
        android:imeSubtypeLocale="en_US"
        android:languageTag="en-US"
        android:imeSubtypeMode="keyboard"
        android:isAsciiCapable="true" />
    <subtype
        android:label="@string/subtype_he"
        android:imeSubtypeLocale="iw"
        android:languageTag="he-IL"
        android:imeSubtypeMode="keyboard" />
</input-method>
